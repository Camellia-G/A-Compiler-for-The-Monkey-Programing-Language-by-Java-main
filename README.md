# A-Compiler-for-The-Monkey-Programing-Language-by-Java-main

实现了数组、表达式的解析、以及let语句、return语句、if-else语句、以及自定义函数、内置函数功能。  
当然也实现了对数据类型的解析，如字符串类型，整数类型等  
 

such as：  
let a=55  
let b=true  
let str="monkey"  
puts(a,b,str)  

let add=fn(a,b,c){  
  return a+b+c    
}  

let a=["ab","c"]  
let b=[a,1,2]  

